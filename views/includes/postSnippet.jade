div.post
  h2.page-header
    a(href=post.url)
      = post.title
    span.date #{post.date.getFullYear()}
  p.preview
    = post.row
  div.tags
    - each tag in post.tags 
      span.label.label-ric #{tag}