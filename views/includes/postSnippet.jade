div.post
  h2
    a(href=post.url)
      = post.title
    span.date #{post.date.getFullYear()}
    div.pimg 
        img(src=post.pimg)

  p.preview
    = post.row
  div.tags
    - each tag in post.tags 
      a(href=tagUrl(tag)).label.label-ric #{tag}